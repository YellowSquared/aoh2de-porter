<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-feature android:glEsVersion="0x00020000" android:required="true"/>
    <uses-feature android:name="android.hardware.type.pc" android:required="false" />

    <!-- android:extractNativeLibs="true" used to be set here. It was a crutch: libGDX 1.10.0's
         natives are 4 KB page-aligned, and extracting them at install time is what earned the app
         PAGE_SIZE_APP_COMPAT_FLAG_LOAD_NOT_ALIGNED on a 16 KB-page device. libGDX 1.13.5 ships
         16 KB-aligned .so files, so the libs can now be mapped straight out of the APK — which is
         what the 16 KB requirement actually asks for, and saves the on-disk second copy. -->
    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher"
        android:isGame="true"
        android:appCategory="game"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:hardwareAccelerated="true"
        tools:ignore="UnusedAttribute"
        android:theme="@style/GdxTheme">

        <!-- Entry point. Unpacks assets.zip on first run behind a progress bar, then starts
             AndroidLauncher. Extraction cannot live in AndroidLauncher itself: that class
             extends AndroidApplication, whose onResume() dereferences state that only exists
             once initialize() has run, so deferring initialize() until the unpack finishes
             crashes on resume. An activity with an intent-filter must be exported on API 31+. -->
        <activity
            android:name="aoc.kingdoms.lukasz.jakowski.android.SplashActivity"
            android:label="@string/app_name"
            android:screenOrientation="landscape"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name="aoc.kingdoms.lukasz.jakowski.android.AndroidLauncher"
            android:label="@string/app_name"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="stateHidden|adjustPan"
            android:resizeableActivity="false"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenSize|screenLayout|smallestScreenSize|fontScale"
            android:exported="false" />
    </application>
</manifest>
